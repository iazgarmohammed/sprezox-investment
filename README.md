# Sprezox — MVP

India-focused startup-investor marketplace (Companies Act 2013, Section 42 compliant).

## Structure
- `apps/web` — Next.js frontend (Vercel)
- `apps/api` — NestJS backend (Railway)
- `packages/shared` — Shared types, enums, DTOs
- `prisma` — Database schema and migrations