{
  "name": "sprezox-investment",
  "version": "1.0.0",
  "private": true,
  "description": "Sprezox - India-focused startup-investor marketplace (MVP)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run start:dev --workspace=apps/api"
  },
  "devDependencies": {
    "@types/multer": "^2.1.0"
  }
}
